import React from "react";

export const ArrowDown = ({className, ...props}) => {
    return <i className={`prodoc-chevron-down ${className}`} {...props}></i>;
}

export const AttachIcon = ({className, ...props}) => {
    return <i className={`prodoc-attach ${className}`} {...props}></i>;
}

export const CloneIcon = ({className, ...props}) => {
    return <i className={`prodoc-clone ${className}`} {...props}></i>;
}

export const EditIcon = ({className, ...props}) => {
    return <i className={`prodoc-edit ${className}`} {...props}></i>;
}

export const LoadingIcon = ({className, ...props}) => {
    return <i className={`prodoc-loading ${className}`} {...props}></i>;
}

export const PlusIcon = ({className, ...props}) => {
    return <i className={`prodoc-plus ${className}`} {...props}></i>;
}

export const PlusCircleIcon = ({className, ...props}) => {
    return <i className={`prodoc-plus-circle ${className}`} {...props}></i>;
}

export const PlusSquareIcon = ({ className, ...props }) => {
  return <i className={`prodoc-plus-square ${className}`} {...props}></i>;
};

export const SquareIcon = ({ className, ...props }) => {
  return <i className={`prodoc-square ${className}`} {...props}></i>;
};

export const TrashIcon = ({ className, ...props }) => {
  return <i className={`prodoc-trash ${className}`} {...props}></i>;
};

export const UserIcon = ({ className, ...props }) => {
  return <i className={`prodoc-user ${className}`} {...props}></i>;
};

export const UsersIcon = ({ className, ...props }) => {
  return <i className={`prodoc-users ${className}`} {...props}></i>;
};